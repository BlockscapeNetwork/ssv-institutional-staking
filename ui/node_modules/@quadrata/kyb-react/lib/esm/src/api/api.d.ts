import { FormField, FormFile, FormFileType } from '../store/features/form/form.slice';
export interface GetUploadUrlQueryParams {
    kybId: string;
    fileName: string;
    backendUrl: string;
    contentType: string;
    applicationId: string;
}
export interface UploadTextFileParams {
    fields: {
        [key in FormField]: string;
    };
    kybId: string;
    backendUrl: string;
    applicationId: string;
}
export interface SubmitKYBFromParams {
    files: {
        [key in FormFileType]: FormFile | undefined;
    };
    fields: {
        [key in FormField]: string;
    };
    kybId: string;
    backendUrl: string;
    applicationId: string;
}
export declare const getUploadURL: (params: GetUploadUrlQueryParams) => Promise<string>;
export declare const uploadFile: (uploadURL: string, objectURL: string) => Promise<void>;
export declare const generateAndUploadFormFieldsJson: ({ fields, ...params }: UploadTextFileParams) => Promise<void>;
export declare const submitKYBForm: (params: SubmitKYBFromParams) => Promise<void>;
