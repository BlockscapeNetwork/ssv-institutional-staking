import { jsx as _jsx } from "react/jsx-runtime";
import { createRef } from 'react';
import KYCField, { KYCFieldType } from './KYCField';
export var generateFields = function () {
    var Fields = {};
    Object.values(KYCFieldType).forEach(function (typeString) {
        var type = typeString;
        var ref = createRef();
        var dom = _jsx(KYCField, { type: type }, type);
        Fields[type] = { dom: dom, ref: ref, type: type };
    });
    return Fields;
};
