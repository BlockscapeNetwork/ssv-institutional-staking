import React from 'react';
export declare type CheckboxInputFieldChangeEventHandler = ({ id, value }: {
    id: string;
    value: boolean;
}) => void;
export interface CheckboxInputFieldProps {
    id: string;
    value: boolean | undefined;
    children: React.ReactNode;
    onChange: CheckboxInputFieldChangeEventHandler;
    extendedClassName?: string;
}
export declare const CheckboxInputField: React.FC<CheckboxInputFieldProps>;
