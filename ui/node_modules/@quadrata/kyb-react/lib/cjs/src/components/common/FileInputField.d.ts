import React from 'react';
import { FormFile } from '../../store/features/form/form.slice';
export declare type FileInputFieldChangeEventHandler = ({ id, value }: {
    id: string;
    value?: FormFile;
}) => void;
export interface TextInputFieldProps {
    id: string;
    error?: string;
    label: string;
    value: FormFile | undefined;
    onChange: FileInputFieldChangeEventHandler;
    onRemove: (id: string) => void;
    required?: boolean;
    description?: string;
}
export declare const FileInputField: React.FC<TextInputFieldProps>;
