import React from 'react';
export declare type TextInputFieldChangeEventHandler = ({ id, value }: {
    id: string;
    value: string;
}) => void;
export declare enum TextInputType {
    TEXT = "text",
    EMAIL = "email",
    TEXTAREA = "textarea",
    PHONE = "tel"
}
export interface TextInputFieldProps {
    id: string;
    label: string;
    type?: TextInputType;
    value: string;
    error?: string;
    onChange: TextInputFieldChangeEventHandler;
    required?: boolean;
    description?: string;
}
export declare const TextInputField: React.FC<TextInputFieldProps>;
