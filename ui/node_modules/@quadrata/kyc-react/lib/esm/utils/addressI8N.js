import AddressFormatter from './address';
import { store } from '../src/store/store';
var I8NAddressFormatter = new AddressFormatter('EN');
export default I8NAddressFormatter;
export var getCountryByCode = function (code) {
    var countries = store.getState().localization.allCountries;
    var target = {};
    countries.forEach(function (country) {
        if (country.code === code) {
            target = country;
        }
    });
    return target;
};
export var getProvinceByCode = function (country, code) {
    var province = {};
    country.zones.forEach(function (zone) {
        if (zone.code === code) {
            province = zone;
        }
    });
    return province;
};
