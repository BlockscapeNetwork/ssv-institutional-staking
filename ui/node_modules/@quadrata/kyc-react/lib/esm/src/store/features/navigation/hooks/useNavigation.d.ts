import { Page, PageError } from '../navigation.slice';
import { APIResponseContractParams, APIResponseItemVeriffSession } from '../../../../controllers/api/beginApplication.api';
export declare const useNavigation: () => {
    page: Page | PageError;
    navigate: (page: Page | PageError) => void;
    resetNavigation: () => void;
    navigateByStatus: ({ veriffSession, contractParameters, error, }: {
        veriffSession?: APIResponseItemVeriffSession | undefined;
        contractParameters?: APIResponseContractParams | undefined;
        error?: string | undefined;
    }) => void;
};
