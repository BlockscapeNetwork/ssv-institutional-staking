var _a;
import { createSlice } from '@reduxjs/toolkit';
var generateInitialState = function () { return ({
    currentCountry: {},
    allCountries: [],
}); };
export var slice = createSlice({
    name: 'LocalizationSlice',
    initialState: generateInitialState(),
    reducers: {
        updateCurrentCountry: function (state, action) {
            state.currentCountry = action.payload;
        },
        fetchAllCountries: function (state, action) {
            state.allCountries = action.payload;
        },
        reset: function (state) {
            state = generateInitialState();
            return state;
        },
    },
});
export var updateCurrentCountry = (_a = slice.actions, _a.updateCurrentCountry), fetchAllCountries = _a.fetchAllCountries, reset = _a.reset;
export default slice.reducer;
