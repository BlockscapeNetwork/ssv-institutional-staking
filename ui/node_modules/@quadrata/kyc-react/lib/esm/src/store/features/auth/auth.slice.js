var _a;
import { createSlice } from '@reduxjs/toolkit';
import jwtDecode from 'jwt-decode';
var generateInitialState = function () { return ({
    jwt: '',
    account: '',
    customerId: '',
}); };
var authSlice = createSlice({
    name: 'auth-slice',
    initialState: generateInitialState(),
    reducers: {
        setAuth: function (state, action) {
            var decoded = jwtDecode(action.payload);
            state.jwt = action.payload;
            state.account = decoded.identity.wallet_address;
        },
        reset: function (state) {
            state = generateInitialState();
            return state;
        },
        setCustomerId: function (state, action) {
            state.customerId = action.payload;
        },
    },
});
export var setAuth = (_a = authSlice.actions, _a.setAuth), reset = _a.reset, setCustomerId = _a.setCustomerId;
export default authSlice.reducer;
