import React from 'react';
export declare const PageSignWallet: React.FC<{
    jwt: string;
    onSign: (hashedMessage: Uint8Array) => void;
    account: string;
    children?: React.ReactNode;
    signature?: string;
    backendUrl: string;
}>;
