var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useEffect } from 'react';
import { useNavigation } from '../store/features/navigation/hooks/useNavigation';
import { Page } from '../store/features/navigation/navigation.slice';
import { PageLayout } from '../components/PageLayout';
export var PageMintNFTStatus = function (_a) {
    var mintComplete = _a.mintComplete, etherscanUrl = _a.etherscanUrl, transactionHash = _a.transactionHash, children = _a.children;
    var navigate = useNavigation().navigate;
    var etherscanTxUrl = "".concat(etherscanUrl, "/tx/").concat(transactionHash);
    useEffect(function () {
        if (mintComplete) {
            navigate(Page.MINT_NFT_COMPLETE);
        }
    }, [navigate, mintComplete]);
    return (_jsxs(PageLayout, { children: [_jsx("h1", { children: "Please wait while we mint your Web3 Identity Passport." }), _jsxs("section", { children: ["This may take a few minutes. You can see the transaction in", ' ', _jsx("a", __assign({ href: etherscanTxUrl, target: "_blank", rel: "noopener noreferrer" }, { children: "Etherscan" })), "."] }), children] }));
};
