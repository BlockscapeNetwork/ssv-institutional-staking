import { APIResponseContractParams } from './beginApplication.api';
export interface KYCFormResponse {
    contractParameters: APIResponseContractParams;
    error?: string;
}
/**
 * Sends the KYC form to the backend. If Development Bypass is enabled,
 * the desired configurations for ID and AM verification will be sent
 * to the bypass endpoint instead. The form data is read from the redux store
 */
export declare const submitKYCForm: (backendUrl: string, jwt: string) => Promise<KYCFormResponse>;
