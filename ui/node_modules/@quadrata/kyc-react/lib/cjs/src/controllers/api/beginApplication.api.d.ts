export interface APIResponseItemVeriffSession {
    id: string;
    url: string;
    vendorData: string;
    host: string;
    status: string;
    sessionToken: string;
}
export interface APIResponseContractParams {
    governanceAddress: string;
    address: string;
    signature: string;
    tokenId: number;
    aml: string;
    did: string;
    country: string;
    issuedAt: number;
    isBusiness: boolean;
}
export interface APIResponseApplication {
    customerId: string;
    contractParameters?: APIResponseContractParams;
    veriffSession?: APIResponseItemVeriffSession;
    error?: string;
}
export interface BeginApplicationParams {
    jwt: string;
    account: string;
    network?: string;
    signature: string;
    backendUrl: string;
}
export declare const beginApplication: ({ jwt, account, network, signature, backendUrl, }: BeginApplicationParams) => Promise<APIResponseApplication>;
